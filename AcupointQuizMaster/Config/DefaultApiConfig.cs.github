using System;

namespace AcupointQuizMaster.Config
{
    /// <summary>
    /// 默认API配置类 - GitHub版本（隐藏敏感信息）
    /// 注意：此文件为GitHub版本，实际发布版本包含内置API密钥
    /// </summary>
    internal static class DefaultApiConfig
    {
        /// <summary>
        /// 获取默认DeepSeek API密钥
        /// 在生产构建中，这个方法会返回内置的API密钥
        /// 在开发环境中，开发者需要设置自己的API密钥
        /// </summary>
        /// <returns>默认API密钥或空字符串</returns>
        public static string GetDefaultDeepSeekApiKey()
        {
            // 首先尝试从环境变量获取
            var envKey = Environment.GetEnvironmentVariable("DEEPSEEK_API_KEY");
            if (!string.IsNullOrEmpty(envKey))
            {
                return envKey;
            }

            // 开发环境下返回空字符串，需要用户手动配置
            // 生产构建时会被替换为实际的API密钥
            return GetBuildTimeApiKey();
        }

        /// <summary>
        /// 获取构建时API密钥
        /// GitHub版本：开发者需要配置自己的API密钥
        /// 发布版本：包含内置的默认API密钥
        /// </summary>
        private static string GetBuildTimeApiKey()
        {
            // GitHub版本：用户需要配置API密钥
            // 实际发布版本会包含内置密钥
            return "";
        }
    }
}